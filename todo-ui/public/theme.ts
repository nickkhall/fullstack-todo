import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    background: {
      primary: '#353941',
      secondary: '#26282B'
    },
    accent: {
      primary: '#5F85DB',
      secondary: '#90B8F8',
    },
    text: {
      primary: '#000',
      secondary: '#FFF'
    }
  }
})
