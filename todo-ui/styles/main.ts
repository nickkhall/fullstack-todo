export default ({ theme }: any) => ({
  background: theme.palette.dark.linearDiagonal,
  borderRadius: '4px',
  boxShadow: 'inset 0px 0px 50px rgba(0,0,0,0.9)',
  color: theme.palette.text.primary,
  display: 'flex',
  height: '100%',
  padding: 10
})
