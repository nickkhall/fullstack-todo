export default ({ theme }: any) => ({
  alignItems: 'center',
  background: theme.palette.dark.linear,
  borderRadius: 4,
  display: 'flex',
  justifyContent: 'space-between',
  maxHeight: 200,
  padding: 10
})
