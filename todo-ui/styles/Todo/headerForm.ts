export default ({ theme }: any) => ({
  alignItems: 'center',
  background: theme.palette.dark.primary,
  borderRadius: '4px',
  display: 'flex',
  justifyContent: 'flex-end',
  maxHeight: 100,
  marginBottom: 15,
  padding: 10,
  width: '100%'
})
